package com.backend.figth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FeignConfig {
  // Configurações padrão do Feign são suficientes para este caso
  // O Feign já está configurado automaticamente pelo Spring Cloud
}